\newcommand*{\ie}{\textit{i.e.\ }}
\newcommand*{\etc}{\textit{etc.}}
\newcommand*{\eg}{\textit{e.g.\ }}
\newcommand*{\etal}{\textit{et al.\ }}

%mathtool macros. 
%Using starred versions (eg \abs*{}) will add \left-\right for oversized needs 
%Can override for lager sizes too \abs[\Bigg]{}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}
\DeclarePairedDelimiter\bra{\langle}{\rvert}
\DeclarePairedDelimiter\ket{\lvert}{\rangle}
\DeclarePairedDelimiterX\innerp[2]{\langle}{\rangle}{#1,#2}
\DeclarePairedDelimiterX\bket[2]{\langle}{\rangle}{#1\,\delimsize\vert\,\mathopen{}#2}
\DeclarePairedDelimiterX\boket[3]{\langle}{\rangle}%
{#1\,\delimsize\vert\,\mathopen{}#2\,\delimsize\vert\,\mathopen{}#3}
\DeclarePairedDelimiterX\com[2]{[}{]}{#1,#2}

\newcommand*{\ten}[1]{\!\times\!10^{#1}}

\newcommand*{\sw}{\texttt}
\newcommand{\onlinecite}{\citenum}
\newcommand{\lin}[1]{{\skolarlining #1}}
\newcommand{\thought}[1]{{\skolarSC #1}}

%Label markers so there are no need for legends in a plot
\newcommand*{\plotmarker}[2]{
    \begin{tikzpicture}[baseline=-0.7ex]
        \node (0,0) [scale=#1, #2] {};
        \end{tikzpicture}
}
%Same deal but for lines
\newcommand*{\plotline}[1]{
    \begin{tikzpicture}[baseline=-0.7ex]
        \draw [#1] (0,0) -- (0.5,0);
        \end{tikzpicture}
}
